[0:a]showwaves@viz=s=${WIDTH}x${HEIGHT}:mode=p2p:rate=${FPS}:draw=${LINE_DRAW}:scale=${LINE_SCALE}:split_channels=${LINE_SPLIT}:colors=${ACCENT}|${ACCENT},fps=${FPS},format=gbrp,dilation=coordinates=255:enable='gte(${LINE_THICKNESS},2)',dilation=coordinates=255:enable='gte(${LINE_THICKNESS},3)',dilation=coordinates=255:enable='gte(${LINE_THICKNESS},4)',format=yuv420p,setsar=1[${OUT}]
